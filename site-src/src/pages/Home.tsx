/** Systems Ledger reconstruction — English content stays structurally separate from Persian RTL rules. */
import PortfolioPage from "./PortfolioPage";
export default function Home() { return <PortfolioPage locale="en" />; }
